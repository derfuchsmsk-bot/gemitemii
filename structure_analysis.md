# Структурный анализ проблем с генерацией изображений

## 1. Основные компоненты

### `main.py` (FastAPI Entry Point)
- **Проблема**: Инициализация `dp` (Dispatcher) происходит сразу при импорте файла.
- **Детали**: Если `src.handlers.image_gen` импортируется до настройки всех зависимостей, это может вызвать циклические импорты или пропуск важных инициализаций.
- **Проверка**: Логика вебхука и health check в целом ок, но порядок импортов критичен.

### `src/services/vertex_ai.py` (Сервис генерации)
- **Подозрение 1 (Модель)**: Модель `gemini-3-pro-image-preview`.
  - Проверить, доступна ли эта модель в вашей конфигурации (Project/Region). Если нет, будет ошибка 403 или 404.
- **Подозрение 2 (Таймауты)**: `timeout=180.0`. Для генерации изображений через Vertex AI часто нужно больше времени (иногда до 5-6 минут для первого "холодного" запуска), если используется не Dedicated Endpoint.
- **Подозрение 3 (Обработка ответа)**: Код ищет `inline_data`. Если модель возвращает только текст (или ошибку в тексте), `image_bytes` останется `None` и выбросится исключение.

### `src/handlers/image_gen.py` (Обработчики)
- **Проблема**: `GenStates` (FSM).
  - Пользователь должен быть в состоянии `GenStates.prompt_wait` для генерации.
  - Если состояние не установлено или сбросилось, обработчик не сработает.
- **Подозрение**: Логика `process_image_prompt`.
  - Формирование `full_user_prompt`. Если `magic_prompt` сломан, промпт может быть пустым или некорректным.
  - Обработка исключений в блоке `try/except`. Если ошибка логируется, но пользователь не видит сообщение (например, `msg.edit_text` падает), это выглядит как "просто зависло".

### `src/middlewares/throttling.py` (Промежуточное ПО)
- **Проблема**: `limit = 1.0`.
  - Это жесткое ограничение (1 сообщение в секунду).
  - Если пользователь быстро отправляет запросы, они игнорируются без ответа.
  - **Важно**: Для генерации изображений пользователь часто отправляет 2 сообщения подряд (текст + нажатие кнопки), что может попасть под Rate Limit.

### `src/config.py` (Конфигурация)
- **Проверка**: `settings.validate()`. Если `PROJECT_ID` или `GOOGLE_APPLICATION_CREDENTIALS` не настроены, Vertex AI упадет при инициализации.

## 2. Сценарий "Почему не генерируется?"

1. **Сценарий A: Ошибка 403/429/500 от Vertex AI**
   - В логах будет `Vertex AI Error`.
   - В телеграме: "Извините, произошла ошибка..." (если ловится в `process_image_prompt`) или тишина.

2. **Сценарий B: Rate Limit**
   - Пользователь нажимает кнопку "🎨 Nano Banana Pro".
   - Пользователь сразу пишет текст.
   - Middleware отсекает второе сообщение.
   - В логах: **ничего** (обработка прервана до хендлера).
   - В телеграме: Кнопка нажата, состояние `prompt_wait` установлено, но отправка текста ничего не дает.

3. **Сценарий C: Ошибка парсинга ответа модели**
   - Модель вернула текст, но не картинку (или вернула в другом формате).
   - Выбрасывается `ValueError: No image generated`.

4. **Сценарий D: Инициализация модели**
   - Модель `gemini-3-pro-image-preview` недоступна в выбранном регионе (`settings.REGION`).

## 3. План действий (Фиксы)

### Шаг 1: Улучшение логирования (Обязательно)
Нужно добавить детальное логирование в `vertex_ai.py` и `image_gen.py`, чтобы видеть, где именно падает запрос (вход, модель, выход, отправка).

### Шаг 2: Проверка доступности модели
В `vertex_ai.py` нужно проверить, что модель инициализируется корректно. Если `gemini-3-pro-image-preview` недоступна, заменить на рабочую (например, `gemini-2.0-flash-exp` или другую доступную модель генерации изображений).

### Шаг 3: Блокировка Rate Limit для генерации
В `throttling.py` нужно исключить Middleware для callback-кнопок или увеличить лимит для процесса генерации, чтобы не блокировать цепочку действий (кнопка -> состояние -> текст).

### Шаг 4: Обработка ошибок в Image Gen Handler
В `image_gen.py` нужно гарантировать, что пользователь получает обратную связь даже если `msg.delete()` или `msg.edit_text()` падает.

### Шаг 5: Проверка таймаутов
Увеличить таймауты в `vertex_ai.py`, если генерация длится долго.

## 4. Итоговая структура исправлений

Файл `main.py` менять не обязательно, если импорты корректны.
Основные изменения будут в:
1. `src/services/vertex_ai.py` (Логи, таймауты, безопасность получения изображения).
2. `src/handlers/image_gen.py` (Улучшенная обработка ошибок и FSM).
3. `src/middlewares/throttling.py` (Исключение callback-ов из лимитов).
4. `src/config.py` (Проверка ключевых переменных окружения).
